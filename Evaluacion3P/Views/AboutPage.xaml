<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Evaluacion3P.Views.AboutPage"
             Title="AboutPage">
    <StackLayout Padding="10">
        <Image Source="favorite_character.png" Aspect="AspectFit" HeightRequest="200"/>
        <Entry Placeholder="Ingresa tu nombre" Text="{Binding UserName}" />
        <Editor Placeholder="Escribe por qué te gusta este personaje" Text="{Binding UserReason}" />
        <Button Text="Guardar" Command="{Binding SaveReasonCommand}" />
        <Label Text="{Binding SavedReason}" TextColor="Black" FontSize="Medium" Margin="10"/>
    </StackLayout>
</ContentPage>